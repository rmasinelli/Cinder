# AI Relay Secret Smoke Test

This harmless documentation-only change exists to verify that the AI PR Relay can see its repository secret and invoke Claude from a pull request created after the secret diagnostic workflow was merged.
