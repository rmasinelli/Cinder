# Historical SQL only

These files document the one-off schema and policy patches used before Issue
#9 consolidated Cinder's database history. They are outside
`supabase/migrations/` deliberately and must not be applied during setup or
deployment.

Use the ordered migration path documented in `docs/database/migrations.md`.
