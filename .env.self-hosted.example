# Optional overrides. If left empty, the existing production Supabase defaults
# in src/lib/supabase.js are compiled into the pilot image.
CINDER_BIND_IP=10.10.40.180
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
